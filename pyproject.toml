[build-system]
requires = ["maturin"]
build-backend = "maturin"

[package]
name = "test_pro"
version = "0.1.0"
edition = "2021"

[lib]
name = "fibonacci"
crate-type = ["cdylib", "rlib"]

[dependencies]
pyo3 = { version = "0.20.3", features = ["extension-module"] }
